/**
 * Permission module map — built from `src/config/systemModules.js`.
 * Do not duplicate module keys here; edit systemModules.js instead.
 */

const { buildModulesMap } = require('../config/systemModules');

module.exports = buildModulesMap();
