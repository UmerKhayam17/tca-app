# Leave empty to use the Vite dev proxy (`/api` → backend on port 5000).
# Set only if the API is on another origin (cookies for refresh may require same-site setup).
# Socket.io (backend origin, without path). Defaults to http://127.0.0.1:5000
# VITE_SOCKET_URL=http://127.0.0.1:5000
